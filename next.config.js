  /** @type {import('next').NextConfig} */
  const nextConfig = {
    output: 'export',
    basePath: '/StealthYield',
    images: {
      unoptimized: true,
    },
  };

  module.exports = nextConfig;
